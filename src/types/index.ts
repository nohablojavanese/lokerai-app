import { CVState } from '../redux/cvSlice';

export interface CVEditorProps {
  // Add any specific props for CVEditor if needed
}

export interface CVPreviewProps {
  // Add any specific props for CVPreview if needed
}

export type { CVState };